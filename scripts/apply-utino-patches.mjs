import fs from 'node:fs';
function replaceOnce(file,find,replacement,label){let source=fs.readFileSync(file,'utf8');if(source.includes(replacement))return false;if(!source.includes(find)){console.log(`Skipping unavailable patch: ${label}`);return false}source=source.replace(find,replacement);fs.writeFileSync(file,source);return true}
const chat='app/messenger/ChatWorkspace.js';
replaceOnce(chat,'import "./v1.css";','import "./v1.css";\nimport "./utino-enhancements.css";\nimport SupportChat from "./SupportChat";','chat imports');
replaceOnce(chat,'  const [groupOpen, setGroupOpen] = useState(false);\n  const [groupName, setGroupName] = useState("");','  const [groupOpen, setGroupOpen] = useState(false);\n  const [createMenuOpen, setCreateMenuOpen] = useState(false);\n  const [channelOpen, setChannelOpen] = useState(false);\n  const [channelName, setChannelName] = useState("");\n  const [channelDescription, setChannelDescription] = useState("");\n  const [channelUsername, setChannelUsername] = useState("");\n  const [channelPublic, setChannelPublic] = useState(true);\n  const [groupName, setGroupName] = useState("");\n  const [editingMessage, setEditingMessage] = useState(null);','chat states');
replaceOnce(chat,'db.from("conversations").select("id,type,title,created_by,created_at")','db.from("conversations").select("id,type,title,created_by,created_at,is_channel,description,channel_username,is_public")','conversation fields');
replaceOnce(chat,'  async function addMember(user) {',`  async function createChannel() {\n    const title = channelName.trim(); if (!title || busy) return; setBusy(true); setError("");\n    const {data,error:e}=await db.rpc("create_channel_conversation",{channel_title:title,channel_description:channelDescription.trim(),channel_username:channelUsername.trim()||null,channel_public:channelPublic});\n    setBusy(false); if(e||!data){setError(e?.message||t.error);return;} setChannelOpen(false);setChannelName("");setChannelDescription("");setChannelUsername("");setChannelPublic(true);await loadConversations();setSelectedId(data);setMobileSidebar(false);\n  }\n  async function editMessage(){if(!editingMessage||!text.trim())return;setBusy(true);const{error:e}=await db.rpc("edit_message",{message_uuid:editingMessage.id,new_content:text.trim()});setBusy(false);if(e){setError(e.message||t.error);return;}setEditingMessage(null);setText("");await loadSelected(selected.id);await loadConversations()}\n  async function removeMessage(id){if(!window.confirm("این پیام حذف شود؟"))return;const{error:e}=await db.rpc("delete_message",{message_uuid:id});if(e)setError(e.message||t.error);else{setEditingMessage(null);await loadSelected(selected.id);await loadConversations()}}\n\n  async function addMember(user) {`,'channel and message actions');
replaceOnce(chat,'<div className="uc-sidebar-tools"><button className="uc-new-group" onClick={() => setGroupOpen(true)}><Icon name="plus" size={17}/>{t.newGroup}</button></div>',`<div className="uc-sidebar-tools uc-create-tools"><button className="uc-new-group uc-create-trigger" onClick={()=>setCreateMenuOpen(v=>!v)}><Icon name="plus" size={16}/><span>جدید</span></button>{createMenuOpen&&<div className="uc-create-menu"><button type="button" onClick={()=>{setCreateMenuOpen(false);document.querySelector(".uc-search-wrap input")?.focus()}}>افزودن کاربر / شروع گفتگو</button><button type="button" onClick={()=>{setCreateMenuOpen(false);setGroupOpen(true)}}>ساخت گروه</button><button type="button" onClick={()=>{setCreateMenuOpen(false);setChannelOpen(true)}}>ساخت کانال</button></div>}</div>`,'create menu');
replaceOnce(chat,'{groupOpen && <Modal onClose={() => { setGroupOpen(false); setGroupSelection([]); }} title={t.create}>',`{channelOpen&&<Modal onClose={()=>setChannelOpen(false)} title="ساخت کانال"><div className="uc-channel-form"><label>نام کانال<input value={channelName} onChange={e=>setChannelName(e.target.value)} maxLength={80}/></label><label>شناسه<input dir="ltr" value={channelUsername} onChange={e=>setChannelUsername(e.target.value.replace(/[^a-z0-9_]/gi,"").toLowerCase())} maxLength={32}/></label><label>توضیحات<textarea value={channelDescription} onChange={e=>setChannelDescription(e.target.value)} maxLength={500}/></label><label><input type="checkbox" checked={channelPublic} onChange={e=>setChannelPublic(e.target.checked)}/> کانال عمومی</label><div className="uc-modal-actions"><button onClick={()=>setChannelOpen(false)}>لغو</button><button className="primary" disabled={busy||!channelName.trim()} onClick={createChannel}>ساخت کانال</button></div></div></Modal>}{groupOpen && <Modal onClose={() => { setGroupOpen(false); setGroupSelection([]); }} title={t.create}>`,'channel modal');
replaceOnce(chat,'{m.message_type === "file" ? <FileMessage message={m} getUrl={getFileUrl} t={t}/> : <div className="uc-message-text">{m.content?.split(/(@[a-z0-9_]{3,20})/gi).map((part, idx) => /^@[a-z0-9_]{3,20}$/i.test(part) ? <span className="uc-mention" key={idx}>{part}</span> : <span key={idx}>{part}</span>)}</div>}',`{m.deleted_at?<div className="uc-message-text uc-message-deleted">این پیام حذف شده است.</div>:m.message_type === "file" ? <FileMessage message={m} getUrl={getFileUrl} t={t}/> : <div className="uc-message-text">{m.content?.split(/(@[a-z0-9_]{3,20})/gi).map((part, idx) => /^@[a-z0-9_]{3,20}$/i.test(part) ? <span className="uc-mention" key={idx}>{part}</span> : <span key={idx}>{part}</span>)}</div>}`,'message deleted state');
replaceOnce(chat,'<div className="uc-message-meta"><time>{timeOf(m.created_at, lang)}</time>',`<div className="uc-message-meta">{m.edited_at&&!m.deleted_at&&<small>ویرایش‌شده</small>}<time>{timeOf(m.created_at, lang)}</time>`,'edited label');
replaceOnce(chat,'<div className="uc-message-wrap">{showSender',`<div className="uc-message-wrap">{mine&&!m.deleted_at&&<div className="uc-message-actions"><button onClick={()=>{setEditingMessage(m);setText(m.content||"")}}>ویرایش</button><button onClick={()=>removeMessage(m.id)}>حذف</button></div>}{showSender`,'message actions');
replaceOnce(chat,'<div className="uc-composer"><button onClick={() => fileRef.current?.click()}',`<div className="uc-composer">{editingMessage&&<div className="uc-editing">در حال ویرایش پیام <button onClick={()=>{setEditingMessage(null);setText("")}}>لغو</button></div>}<button onClick={() => fileRef.current?.click()}`,'edit composer');
replaceOnce(chat,'<button className="uc-send" onClick={sendMessage} disabled={busy || !text.trim()}','<button className="uc-send" onClick={editingMessage?editMessage:sendMessage} disabled={busy || !text.trim()}','edit send');
replaceOnce(chat,'{supportOpen && <Modal onClose={() => setSupportOpen(false)} title={t.supportOfficial}>',`{supportOpen&&<SupportChat onClose={()=>setSupportOpen(false)}/>} {supportOpen && false && <Modal onClose={() => setSupportOpen(false)} title={t.supportOfficial}>`,'direct support');
const admin='app/admin/page.js';
replaceOnce(admin,'import "./admin.css";','import "./admin.css";\nimport SupportTickets from "./SupportTickets";\nimport SupportInbox from "./SupportInbox";\nimport AdminMessages from "./AdminMessages";','admin imports');
replaceOnce(admin,'{tab==="messages"&&<div className="admin-card">','{tab==="tickets"&&<><SupportInbox/><SupportTickets/></>}\n{tab==="messages"&&<><AdminMessages/></>}\n{tab==="messages"&&false&&<div className="admin-card">','admin panels');
console.log('Utino final patches completed');
