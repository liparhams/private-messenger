import fs from 'node:fs';
function patch(file,find,repl,label){let s=fs.readFileSync(file,'utf8');if(s.includes(repl))return;if(!s.includes(find)){console.log('skip',label);return}s=s.replace(find,repl);fs.writeFileSync(file,s)}
const chat='app/messenger/ChatWorkspace.js';
patch(chat,'import "./v1.css";','import "./v1.css";\nimport "./utino-enhancements.css";\nimport SupportChat from "./SupportChat";','imports');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop2');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop3');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop4');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop5');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop6');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop7');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop8');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop9');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop10');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop11');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop12');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop13');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop14');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupOpen, setGroupName] = useState("");','noop15');
patch(chat,'  const [groupOpen, setGroupName] = useState("");','  const [groupName, setGroupName] = useState("");','noop16');
patch(chat,'db.from("conversations").select("id,type,title,created_by,created_at")','db.from("conversations").select("id,type,title,created_by,created_at,is_channel,description,channel_username,is_public")','fields');
patch(chat,'db.from("messages").select("id,conversation_id,sender_id,receiver_id,content,file_url,file_type,file_name,message_type,created_at,reactions")','db.from("messages").select("id,conversation_id,sender_id,receiver_id,content,file_url,file_type,file_name,message_type,created_at,reactions,edited_at,deleted_at")','message metadata');
patch(chat,'  async function addMember(user) {',`  async function createChannel(){const title=channelName.trim();if(!title||busy)return;setBusy(true);const{data,error:e}=await db.rpc("create_channel_conversation",{channel_title:title,channel_description:channelDescription.trim(),channel_username:channelUsername.trim()||null,channel_public:channelPublic});setBusy(false);if(e||!data){setError(e?.message||t.error);return}setChannelOpen(false);setChannelName("");setChannelDescription("");setChannelUsername("");setChannelPublic(true);await loadConversations();setSelectedId(data);setMobileSidebar(false)}\n  async function editMessage(){if(!editingMessage||!text.trim())return;setBusy(true);const{error:e}=await db.rpc("edit_message",{message_uuid:editingMessage.id,new_content:text.trim()});setBusy(false);if(e){setError(e.message||t.error);return}setEditingMessage(null);setText("");await loadSelected(selected.id);await loadConversations()}\n  async function removeMessage(id){if(!window.confirm("این پیام حذف شود؟"))return;const{error:e}=await db.rpc("delete_message",{message_uuid:id});if(e)setError(e.message||t.error);else{setEditingMessage(null);await loadSelected(selected.id);await loadConversations()}}\n\n  async function addMember(user) {`,'actions');
patch(chat,'<div className="uc-sidebar-tools"><button className="uc-new-group" onClick={() => setGroupOpen(true)}><Icon name="plus" size={17}/>{t.newGroup}</button></div>',`<div className="uc-sidebar-tools uc-create-tools"><button className="uc-new-group uc-create-trigger" onClick={()=>setCreateMenuOpen(v=>!v)}><Icon name="plus" size={16}/>جدید</button>{createMenuOpen&&<div className="uc-create-menu"><button type="button" onClick={()=>{setCreateMenuOpen(false);document.querySelector(".uc-search-wrap input")?.focus()}}>افزودن کاربر / شروع گفتگو</button><button type="button" onClick={()=>{setCreateMenuOpen(false);setGroupOpen(true)}}>ساخت گروه</button><button type="button" onClick={()=>{setCreateMenuOpen(false);setChannelOpen(true)}}>ساخت کانال</button></div>}</div>`,'menu');
patch(chat,'{groupOpen && <Modal onClose={() => { setGroupOpen(false); setGroupSelection([]); }} title={t.create}>',`{channelOpen&&<Modal onClose={()=>setChannelOpen(false)} title="ساخت کانال"><div className="uc-channel-form"><label>نام کانال<input value={channelName} onChange={e=>setChannelName(e.target.value)} maxLength={80}/></label><label>شناسه<input dir="ltr" value={channelUsername} onChange={e=>setChannelUsername(e.target.value.replace(/[^a-z0-9_]/gi,"").toLowerCase())} maxLength={32}/></label><label>توضیحات<textarea value={channelDescription} onChange={e=>setChannelDescription(e.target.value)} maxLength={500}/></label><label><input type="checkbox" checked={channelPublic} onChange={e=>setChannelPublic(e.target.checked)}/> کانال عمومی</label><div className="uc-modal-actions"><button onClick={()=>setChannelOpen(false)}>لغو</button><button className="primary" disabled={busy||!channelName.trim()} onClick={createChannel}>ساخت کانال</button></div></div></Modal>}{groupOpen && <Modal onClose={() => { setGroupOpen(false); setGroupSelection([]); }} title={t.create}>`,'channel modal');
patch(chat,'<div className="uc-chat-copy"><strong>{name}</strong>',`<div className="uc-chat-copy"><strong>{name} <small className="uc-type-badge">{c.is_channel?"کانال":c.type==="group"?"گروه":""}</small></strong>`,'type badges');
patch(chat,'<div className="uc-message-wrap">{showSender',`<div className="uc-message-wrap">{mine&&!m.deleted_at&&<div className="uc-message-actions"><button onClick={()=>{setEditingMessage(m);setText(m.content||"")}}>ویرایش</button><button onClick={()=>removeMessage(m.id)}>حذف</button></div>}{showSender`,'message controls');
patch(chat,'<div className="uc-message-meta"><time>{timeOf(m.created_at, lang)}</time>',`<div className="uc-message-meta">{m.edited_at&&!m.deleted_at&&<small>ویرایش‌شده</small>}<time>{timeOf(m.created_at, lang)}</time>`,'edited');
patch(chat,'{m.message_type === "file" ? <FileMessage message={m} getUrl={getFileUrl} t={t}/> : <div className="uc-message-text">','{m.deleted_at?<div className="uc-message-text">این پیام حذف شده است.</div>:m.message_type === "file" ? <FileMessage message={m} getUrl={getFileUrl} t={t}/> : <div className="uc-message-text">','deleted');
patch(chat,'<div className="uc-composer"><button onClick={() => fileRef.current?.click()}',`<div className="uc-composer">{editingMessage&&<div className="uc-editing">در حال ویرایش <button onClick={()=>{setEditingMessage(null);setText("")}}>لغو</button></div>}<button onClick={() => fileRef.current?.click()}`,'edit composer');
patch(chat,'<button className="uc-send" onClick={sendMessage} disabled={busy || !text.trim()}',`<button className="uc-send" onClick={editingMessage?editMessage:sendMessage} disabled={busy || !text.trim()}`,'edit send');
patch(chat,'{supportOpen && <Modal onClose={() => setSupportOpen(false)} title={t.supportOfficial}>',`{supportOpen&&<SupportChat onClose={()=>setSupportOpen(false)}/>} {supportOpen && false && <Modal onClose={() => setSupportOpen(false)} title={t.supportOfficial}>`,'support');
const admin='app/admin/page.js';
patch(admin,'import "./admin.css";','import "./admin.css";\nimport SupportTickets from "./SupportTickets";\nimport SupportInbox from "./SupportInbox";\nimport AdminMessages from "./AdminMessages";','admin imports');
patch(admin,'{tab==="messages"&&<div className="admin-card">','{tab==="tickets"&&<><SupportInbox/><SupportTickets/></>}\n{tab==="messages"&&<AdminMessages/>}\n{tab==="messages"&&false&&<div className="admin-card">','admin panels');
function rename(dir){if(!fs.existsSync(dir))return;for(const e of fs.readdirSync(dir,{withFileTypes:true})){const p=`${dir}/${e.name}`;if(e.isDirectory())rename(p);else if(/\.(js|jsx|ts|tsx|json|css|md|html)$/.test(e.name)){const s=fs.readFileSync(p,'utf8'),n=s.replaceAll('Utino Chat v1','UTINOCHATV1');if(n!==s)fs.writeFileSync(p,n)}}}
rename('app');rename('public');
console.log('UTINOCHATV1 patches complete');
